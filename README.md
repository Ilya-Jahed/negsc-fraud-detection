# negsc-fraud-detection
Reimplementation of NEGSC: a Node-Edge Graph Attention encoder (NEGAT) with graph-contrastive self-supervised learning (Wasserstein + Gromov-Wasserstein distance losses) for network intrusion detection on NetFlow data (NF-BoT-IoT-v2).
